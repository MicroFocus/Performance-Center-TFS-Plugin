/**
 * XmlUtils — placeholder file.
 *
 * XML serialization helpers are currently co-located in src/models/index.ts as
 * toXml() methods on LreRunRequestXml, LreAuthenticationClientXml, etc.
 * XML parsing is handled by the XMLParser instance inside LreClient.
 *
 * If XML utilities grow (e.g. namespace handling, schema validation) this file
 * is the right place for them.  Until then nothing lives here.
 */
